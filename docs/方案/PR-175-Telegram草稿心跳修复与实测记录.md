# PR #175 Telegram 草稿心跳修复与实测记录

日期：2026-09-10，Asia/Taipei。

## 合并与贡献归属

- 先以 merge commit `91822cb` 合并 PR #175，保留作者原始提交 `583bfc9160f06893f60309b5603f481efa166d48`，随后单独修复心跳积压。
- 原始提交的作者邮箱已被 GitHub 识别为 `@geekyfoxlab`；不 squash 或改写作者历史。
- `package.json` contributors 和 CHANGELOG 同步注明作者贡献。

## 最小修复

只调整共享桥的心跳调度：前一次心跳尚未完成时跳过后续 tick，等待 typing 和 draft 两个请求都结算后再允许下一次心跳。开始最终答复或错误投递前停止定时器，外层 finally 继续负责兜底清理。

沿用 PR 原有的 Telegram 写入队列，保证在途草稿完成后才发送最终帧。不增加依赖、用户设置、存储状态或新的队列。

## 自动化验证

- 原 PR 整合到当时最新 main：`npm run check`，2607 项通过。
- 新增三个回归用例在修复前全部失败：慢请求场景发送了 12 次草稿而非预期的 2 次；成功和失败收尾期间仍继续刷新。
- 修复后 Telegram 和共享桥针对性测试：147 项通过。
- 修复后 `npm run check`：2610 项通过，构建和包校验通过。
- 新用例覆盖慢 Draft 请求、typing 失败时仍等待在途 Draft、刷新失败后的恢复，以及最终或错误投递仍在途时停止心跳。

## 本机 Telegram 实测

运行环境为本机 DSH `0.1.5-alpha.2`，加载包含修复的 dsh-im `4.18.0` 开发构建。重启本机 Host 后，使用用户指定的“今天是梁子”机器人测试。

1. 从已登录的 Telegram 客户端发送 `/version`，机器人回复 `dsh-im v4.18.0`。
2. 发送验收消息，要求终端执行 `sleep 45`，完成后返回唯一标记。
3. 终端进程实际运行；静默约 38 秒时，Telegram 仍显示“正在使用 bash…”草稿和 typing 状态，超过 Rich Draft 的 30 秒预览窗口。
4. 命令完成后，收到一个包含 `PR175_TELEGRAM_HEARTBEAT_OK_0024` 的最终回复，草稿和 typing 消失。模型还输出了解释文字，因此这里只确认标记送达和生命周期，不把它记作严格的纯标记输出测试。
5. 最后读取本机 Telegram 管理接口，确认连接健康、消息已回复且没有消息错误。

实机验证覆盖完整收发和长时间无新内容时的草稿显示；慢网络与失败调度通过可控回归测试验证，未声称实机遭遇了真实网络故障。
