# 上下文增强来源块独立消息配对方案

> 状态：已实现，自动化回归通过
>
> 日期：2026-09-12
>
> 关联：[上下文增强](../context-enhancement.md)、[机器人消息来源元数据注入方案](机器人消息来源元数据注入方案.md)

## 1. 结论

开启上下文增强后，来源块与增强提示词不再留在用户消息正文里，而是由 Host 在 `agent/pre-step` 拆成独立消息：

- 用户消息 = 用户真正发送的内容（保留原 id 与 durable source）；
- 一条 `source.kind = 'plugin'`（`plugin: 'dsh-im'`、`form: 'notice'`）的来源块消息；
- 一条同样来自 dsh-im 的 `form: 'instructions'` 增强提示词消息（仅在配置了提示词时）。

**配对由消息身份决定，不由队列位置决定。**

## 2. 背景：为什么来源只能在插件侧编码

`session.prompt` 的请求体只有 `requestId / sessionId / mode / content / clientTimeZone`（见 `@deepseek-ai/dsh-api-session-controller` 的 `SessionPromptRequest`），Host 在准入时固定写入 `source = { kind: 'user', rpcId, clientTimeZone? }`。渠道无法在 RPC 上声明"这段内容来自插件"。

因此原方案只能把来源编码进 prompt 文本。代价有三：

1. 会话展示中用户气泡混入裸 `<dsh_im_source>` / `<dsh_im_source_guidance>`；
2. durable log 无法区分"用户说的"与"插件加的"，审计与归因都失真；
3. 会话标题需要 `session-title.mjs` 的 `INJECTED_CONTEXT_PREFIX` 剥离正则兜底。

注意：语义消息（见 `CONTEXT.md`）本身一直包含内容、引用关系与会话位置；本方案改的是它在 Harness 边界的编码方式，不是语义消息的定义。

## 3. 为什么不用 `agent.inject()`

`inject()` 写入 next-step 队列，`session.prompt` 的 `queue` 模式写入 next-turn 队列，而 Agent 每一步 `claim()` 的规则是"取光 next-step + 恰好一条 next-turn"（`@deepseek-ai/dsh-agent-loop`）。因此"先注入 N 条 context、再排队 N 条用户消息"在并发下**必然错配**：

```
next-step  = [C1, C2, C3]
next-turn  = [U1, U2, U3]
第一步 claim → [C1, C2, C3, U1]   // 三个来源都算到 U1 头上
第二步 claim → [U2]                // U2 没有来源
```

更糟的是会话正在运行 turn 时的注入：`inject` 落进**当前** turn 的 next-step，而用户消息排在下一个 turn，来源会被算到上一个用户头上。

dsh-im 对每个会话 key 用 promise 链串行化（`text-harness-bridge.mjs` 的 `#queues`），所以"同一个群聊三个人同时 @"这一条路径恰好不会并发。但只要出现第二条并发入口——同一 Session 被多个会话/机器人绑定、GUI 同时输入、延迟交付、Office/Webhook——错配就会真实发生。**这不是结构性保证，不能用。**

## 4. 机制

1. **渠道侧不变**：`enhanceContextContent` 仍产出 `<dsh_im_source>` / `<dsh_im_source_guidance>` 前缀，`session.prompt` 仍收到带前缀的内容。标签字面量由 `INJECTED_CONTEXT_TAGS` / `INJECTED_CONTEXT_SEPARATOR` 统一导出，生产者与解析器不可能漂移。
2. **Host 侧监听**：`plugin-src/host/injected-context.mjs` 注册 `ctx.on('agent/pre-step', …, { global: true })`。该 waterfall 的 `messages` 正是本步已经 claim、尚未提交的那一批；提交发生在同一同步路径的后续 `session.append('user/message', …)`。先例：`@deepseek-ai/dsh-agent-instructions` 用同一位置插入自己的上下文。
3. **拆分**：`src/channels/shared/injected-context.mjs` 解析消息 content 首个文本部件的前缀。解析是严格的——来源块 JSON 必须可解析（生产者会转义 `<` `>` `&`，因此标签不可能被值闭合），guidance 的闭合标签必须独占一行；任何不匹配一律不改。
4. **替换**：用户消息换成去掉前缀的原文，`id` 与 `source` 原样保留；新增 0..2 条 plugin 来源消息，顺序为 `[用户消息, 来源块, 增强提示词]`。

顺序说明：Host 自身的运行上下文快照也追加在用户消息之后（`[...claimed, context]`），因此本方案与 Host 惯例一致；会话标题只收集 `source.kind === 'user'` 的消息，所以首条人类消息的判定不受影响。

## 5. 保证与边界

| 关注点 | 结论 |
| --- | --- |
| 配对 | 由"这条被 claim 的消息自己携带的前缀"决定，与队列顺序无关；同一步内多条用户消息各自配对 |
| 失败隔离 | 解析失败、重写抛错、`reject` 分支都原样返回原 decision，前缀留在用户消息里（等价旧行为） |
| 空消息 | 去掉前缀后没有任何内容的用户消息不替换，避免提交空消息 |
| 幂等 | 已拆分的批次再次经过重写是 no-op（新消息 `source.kind === 'plugin'`，用户消息已无前缀） |
| 降级 | 没有 Agent 循环的 Host、或 `harnessBaseUrl` 指向外部 Harness 时不派发该事件，行为与旧版一致 |
| 其他插件 | 未改动的消息保持对象身份；被拆分的用户消息会换成新对象，因此按对象身份匹配 claimed 消息的插件（如 `dsh-agent-instructions` 的 `findLastIndex(m => messages.includes(m))`）若在本插件之后运行，插入位置会退化为批次头部——位置合法，顺序与旧版略有差异 |
| 未覆盖 | `dsh_im_reply_to` 引用块仍在用户消息正文；`/补充指令` 的 steer 路径目前不带来源块 |

归因与展示改善后，来源仍是 user role 的内容，抗提示词注入仍需系统级声明与字段最小化（`senderName` / `conversationTitle` 是用户可控文本）。

## 6. 验证

- `test/injected-context.test.mjs`（14 项）：生产者↔解析器无损往返、标签常量防漂移、结构化路径只移除前缀部件、仅 guidance 的前缀、非前缀文本不误伤、摘要回退与上限、一批三条消息各自配对、未携带前缀的消息保持对象身份、空前缀不留空消息、重复重写幂等、installer 的 `enter`/`reject`/抛错/无 ctx 分支。
- 全量回归：改动前后失败集合逐条一致；构建产物 `lib/index.js`、`lib/client.js` 与 `scripts/verify-package.mjs` 均通过。
